{
  "name": "interest-badge-root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start:dev": "npm run start:dev --prefix apps/api",
    "build": "npm run build --prefix apps/api"
  },
  "workspaces": [
    "apps/api"
  ]
}

